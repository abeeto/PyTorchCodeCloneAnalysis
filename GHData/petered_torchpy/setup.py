from setuptools import setup, find_packages

setup(
    name = "torchpy",
    author = "peter",
    version = 0,
    dependency_links = (),
    install_requires = ['numpy'],
    scripts = [],
    packages=find_packages(),
    )
