from setuptools import setup

setup(
    name="graphtorch",
    version="0.0.1",
    description="A Pytorch Graph Neural Network Libarary based on NestedTensor",
    install_requires=["torch"],
)
